name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Executar testes simulados
        run: |
          echo "✅ Nenhum teste implementado ainda."
          echo "🧪 Simulando testes com sucesso!"
          echo "➡️ Em breve podemos usar Jest ou Cypress."